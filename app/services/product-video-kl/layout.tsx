import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Product Video Production KL | Shopee, Lazada & E-commerce Video",
  description: "Product video production in Kuala Lumpur from RM500. Shopee, Lazada & TikTok Shop videos. 360° spins, demos, lifestyle shoots. Get free quotes.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
