import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Drone Videography Selangor | Aerial Video Services & Pricing",
  description: "Licensed drone videography services in Selangor from RM1,500. Property, construction, events & commercial aerial video. CAAM-certified operators.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
