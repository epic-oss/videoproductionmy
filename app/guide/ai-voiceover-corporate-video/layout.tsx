import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Are AI Voiceovers Acceptable for Corporate Videos? | Malaysia Guide",
  description: "AI voiceovers are acceptable for internal training and social media but not recommended for brand films and investor content. Costs, tools, and when to use human talent.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
