import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Education & University Video Production Malaysia | Campus Tours & Recruitment",
  description: "University and school video production in Malaysia from RM5,000. Campus tours, student recruitment, course promos, graduation coverage. Get free quotes.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
