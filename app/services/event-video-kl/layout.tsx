import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Event Videography KL | Kuala Lumpur Event Video Services & Pricing",
  description: "Professional event videography in Kuala Lumpur from RM2,000. Multi-camera, same-day edit, live streaming. Get free quotes from KL event videographers.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
