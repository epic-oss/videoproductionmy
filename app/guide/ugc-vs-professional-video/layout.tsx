import { Metadata } from "next";

export const metadata: Metadata = {
  title: "UGC vs Professional Brand Video Malaysia | What Works Better?",
  description: "Compare user-generated content vs professional brand videos in Malaysia. Authenticity, cost, conversion rates, and best use cases compared side-by-side.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
