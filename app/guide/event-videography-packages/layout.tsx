import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Event Videography Packages & Pricing Malaysia | VideoProductionMY",
  description: "Compare event videography packages in Malaysia from RM2,000-RM15,000+. Basic, standard & premium tiers with pricing breakdown. Get free quotes.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
