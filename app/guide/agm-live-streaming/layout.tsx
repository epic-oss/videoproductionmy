import { Metadata } from "next";

export const metadata: Metadata = {
  title: "AGM Live Streaming Services Malaysia | Pricing & Requirements",
  description: "AGM live streaming services in Malaysia from RM5,000. Bursa-compliant virtual AGM with RPV, voting & Q&A. Technical requirements and pricing guide.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
