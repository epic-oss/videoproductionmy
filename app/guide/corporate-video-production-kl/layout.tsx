import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Corporate Video Production Kuala Lumpur | Pricing & Top Companies",
  description: "Corporate video production in Kuala Lumpur from RM5,000. Compare KL video production companies, pricing, and what to expect. Get free quotes.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
