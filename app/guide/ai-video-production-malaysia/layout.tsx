import { Metadata } from "next";

export const metadata: Metadata = {
  title: "AI Video Production Malaysia | Sora, AI Editing & Voiceover Tools Guide",
  description: "How Malaysian businesses use AI for video production in 2026. Sora, AI voiceovers, AI editing tools, cost savings, limitations, and when to use AI vs traditional production.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
