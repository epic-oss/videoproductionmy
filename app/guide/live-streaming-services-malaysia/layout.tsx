import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Live Streaming Services Malaysia | Corporate Events, AGM & Hybrid Events",
  description: "Professional live streaming services for corporate events in Malaysia. Multi-camera, hybrid events, platforms compared, pricing from RM3,000. Get free quotes.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
