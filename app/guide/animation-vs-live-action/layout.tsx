import { Metadata } from "next";

export const metadata: Metadata = {
  title: "2D Animation vs Live Action Video Malaysia | Which Converts Better?",
  description: "Compare 2D animation vs live action video production in Malaysia. Cost, timeline, engagement rates, and best use cases for SaaS, tech, and corporate videos.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
