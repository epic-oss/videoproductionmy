import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Stock Footage vs Original Shoot Malaysia | When to Save or Spend",
  description: "Compare stock footage vs original video shoots in Malaysia. Cost, uniqueness, brand consistency, and when each option makes sense for your budget.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
