import { Metadata } from "next";

export const metadata: Metadata = {
  title: "FINAS Permit for Corporate Video Malaysia | When Required & How to Apply",
  description: "FINAS permits are required for commercial filming in Malaysia with crews of 15+ or foreign talent. Learn when you need one, costs (RM200–RM1,000), and application process.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
