import { Metadata } from "next";

export const metadata: Metadata = {
  title: "How Long Does Video Production Take in Malaysia? | Timeline Guide",
  description: "Video production in Malaysia takes 2–6 weeks on average. Breakdown: 1–2 weeks pre-production, 1–2 days filming, 1–3 weeks post-production and revisions.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
