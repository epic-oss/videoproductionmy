import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Drone Videography Services Malaysia | Permits, Pricing & Use Cases",
  description: "Drone videography in Malaysia from RM1,500. CAAM permits, pricing for property, construction, events & tourism. Find licensed drone operators.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
