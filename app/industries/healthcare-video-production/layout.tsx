import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Healthcare & Medical Video Production Malaysia | Hospital & Clinic Video",
  description: "Healthcare video production in Malaysia from RM5,000. Hospital tours, doctor profiles, patient testimonials, medical training videos. Get free quotes.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
