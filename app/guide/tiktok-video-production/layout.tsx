import { Metadata } from "next";

export const metadata: Metadata = {
  title: "TikTok & Reels Video Production Malaysia | Short-Form Content Agency Guide",
  description: "TikTok and Instagram Reels video production for Malaysian brands. Vertical format, hook strategies, TikTok Shop integration, pricing from RM1,000 per video.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
