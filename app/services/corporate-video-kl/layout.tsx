import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Corporate Video Production KL | Kuala Lumpur Companies & Pricing",
  description: "Find corporate video production companies in Kuala Lumpur. Compare KL video production pricing from RM5,000. Get free quotes for company profiles, training videos & TVCs.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
